TODO:

main binary:
* mt_instrument_load mostly duplicates mt_instrument_store! Make common code.
* handle memory effects in dirty calls.
* handle read, write syscalls

dummy client:

nvidia client:
* reveng basic nv ioctls and parse results
        (have to replace /proc/*/maps parsing, etc).
* import all information from renoveau
* reveng, reveng, reveng...
